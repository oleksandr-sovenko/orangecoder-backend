GPIO.on('change', function(data) {
    data = DATETIME.format('lll') + ' ' +
         JSON.stringify(data);

    FILE.append( "/tmp/test.txt", data + "\n");
});

// setInterval(function() {
//     CONSOLE.log( DATETIME.format('lll') );
// }, 5000);
